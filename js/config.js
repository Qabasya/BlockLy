/* настройки приложения. Подключается первым. */
window.MKB = window.MKB || {};

// Список наполняют workshops/*.js. Он должен существовать и когда не подключён
// ни один файл: последний мастер-класс могли удалить из админки.
MKB.workshops = MKB.workshops || [];

MKB.config = {
  // Пароль преподавателя для входа в администрирование. Это ширма от
  // любопытных учеников, а не защита: лежит открытым текстом, не хэшируется.
  adminPassword: 'mk2026',

  // Шаг отступа, к которому «Выровнять отступы» приводит код фрагмента
  indent: { arduino: 2, python: 4 }
};
